# Backend Configuration
# The backend server host (default: localhost)
VITE_BACKEND_HOST=localhost

# The backend server port (default: 5000)
VITE_BACKEND_PORT=5000

# API request timeout in milliseconds
# Development: 30000 (30 seconds), Production: 15000 (15 seconds)
VITE_API_TIMEOUT=30000

# RAG Service Configuration
# The RAG service base URL for function data
VITE_RAG_BASE_URL=http://localhost:8000

# Legacy configuration (kept for compatibility)
VITE_API_BASE_URL=/api