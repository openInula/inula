{
  "name": "inula2-rust-compiler",
  "version": "1.0.0",
  "description": "Rust implementation of Inula 2.0 compiler with NAPI and WASM support",
  "scripts": {
    "build:napi": "cd src/napi && npm run build",
    "build:wasm": "cd src/wasm && wasm-pack build --target nodejs --out-dir pkg",
    "build:all": "npm run build:napi && npm run build:wasm",
    "test": "npm run test:unit",
    "test:unit": "cd src/napi && cargo test",
    "benchmark": "node benchmarks/ || true",
    "clean": "rm -rf src/*/target src/*/node_modules src/*/dist src/wasm/pkg"
  },
  "keywords": [
    "inula",
    "compiler",
    "rust",
    "napi",
    "wasm",
    "jsx",
    "react"
  ],
  "author": "Inula Team",
  "license": "MIT",
  "workspaces": [
    "src/napi",
    "src/wasm"
  ],
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}




